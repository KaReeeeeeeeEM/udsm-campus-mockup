export type CardProps = {
  profileName: string;
  timeAgo: string;
  title: string;
  description: string;
  college: string;
};

export type SuggestionProps = {
  username: string;
  title: string;
  description: string;
  avatarSrc: string;
  followers: string;
  following: string;
};

export type ContactProps = {
  campus: string;
  contact: string;
}
